import { LoginForm } from "./LoginForm";
import { StyledMain } from "./style";

export const LoginPage = () => {
  return (
    <StyledMain>
      <section>
        <LoginForm />
      </section>
    </StyledMain>
  );
};
